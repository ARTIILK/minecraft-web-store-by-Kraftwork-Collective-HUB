## Packages
three | Required for the lightweight animated background canvas
zustand | Lightweight global state management for currency toggle
framer-motion | Smooth hover animations for cards and elements

## Notes
Dynamic theme colors from siteConfig are injected into the document root as CSS variables.
Images will attempt to load natively, falling back to Lucide icons if the paths (like /assets/vip.png) are missing.
External checkout links are opened in a new tab for safety.
